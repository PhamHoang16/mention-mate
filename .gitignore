# Credentials — KHÔNG BAO GIỜ commit file này
.env
.env.local
.env.*.local

# Telethon session files — chứa thông tin đăng nhập đã xác thực
*.session
*.session-journal

# Runtime data folder mounted into container
data/

# Release build artifacts
dist/
*.zip

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# GitNexus index
.gitnexus

# AI agent machinery (kept locally for development, not in public repo)
# These exist on the `dev` branch — master stays clean for public users
.claude/
.codex/
.vsaf/
AGENTS.md
CLAUDE.md

# Translation / sed backup files (in case sed -i.bak was used somewhere)
*.bak
*.orig

# OS/editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
